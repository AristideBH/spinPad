# ──────────────────────────────────────────
#  keyboard-firmware — root .gitignore
# ──────────────────────────────────────────

# ── OS ────────────────────────────────────
.DS_Store
.DS_Store?
._*
Thumbs.db
ehthumbs.db
desktop.ini

# ── Éditeurs ──────────────────────────────
.vscode/
.idea/
*.swp
*.swo
*~

# ── ESP-IDF / CMake ───────────────────────
packages/firmware/build/
packages/firmware/sdkconfig
packages/firmware/dependencies.lock
packages/firmware/managed_components/
packages/firmware/.clangd
# Compat ancien chemin
firmware/build/
firmware/sdkconfig
firmware/dependencies.lock
firmware/managed_components/
firmware/.clangd

# ── Node / pnpm ───────────────────────────
node_modules/
.pnpm-store/
coverage/

# ── Studio / SvelteKit ────────────────────
**/.svelte-kit/
**/build/
**/build-embedded/
packages/studio/.env
packages/studio/.env.*
# Compat ancien chemin
config-app/.env

# ── Secrets locaux ────────────────────────
.claude/settings.local.json
.env
.env.*
packages/firmware/.hintrc
.claude/worktrees/*
packages/website/graphify-out/manifest.json
packages/firmware/sdkconfig.old
packages/firmware/sdkconfig.old
